{
  "installCommand": "npm install && npm --prefix client install --include=dev",
  "buildCommand": "npm --prefix client run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/((?!api.*).*)", "destination": "/index.html" }]
}
